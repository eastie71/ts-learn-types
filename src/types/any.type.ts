let firstName: any = "Craig"

firstName = 1234

firstName = ['a', 'b', 'c']

// Can remove the strict typing here via tsconfig.json
// Paramater is required to have a type
/** 
function multByTen(number) {
    return number * 10
}
**/